@model IEnumerable<f12020.Models.temporada>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_temporada }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_temporada }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_temporada })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
