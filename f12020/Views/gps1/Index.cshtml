@model IEnumerable<f12020.Models.gp>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.local_gp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data_gp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.temporada.nome)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.local_gp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data_gp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.temporada.nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_gp }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_gp }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_gp })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
